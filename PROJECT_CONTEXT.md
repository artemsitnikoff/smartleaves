# Smart Leaves (Умные листочки) - Контекст проекта

## 📋 Общая информация

**Название проекта:** Smart Leaves (Умные листочки)
**Тип проекта:** Образовательный портал для детских рабочих листов (аналог kiddoworksheets.com)
**Владелец:** Artem Sitnikov (математик, CEO)
**Статус:** Прототип в разработке
**Язык контента:** Английский (категории и worksheet на английском)

## 🎯 Цель проекта

Создать веб-платформу для публикации и распространения образовательных worksheet (рабочих листов) для детей. Пользователи могут:
- Просматривать каталог worksheet по категориям
- Фильтровать по тегам, уровню сложности, классу
- Просматривать превью worksheet
- Скачивать PDF файлы бесплатно
- Без регистрации пользователей

## 🏗️ Технологический стек

### Backend
- **Framework:** Django 5.0
- **CMS:** Wagtail 6.0
- **API:** Django REST Framework 3.14+
- **Database:** SQLite (dev), PostgreSQL ready (prod)
- **Documentation:** drf-spectacular (Swagger/OpenAPI 3.0)
- **Image processing:** Pillow, pdf2image
- **PDF generation:** reportlab
- **Slug generation:** python-slugify

### Frontend
- **Framework:** Vue.js 3 (Composition API)
- **Language:** TypeScript
- **Build tool:** Vite 7.3.1
- **Routing:** Vue Router 4
- **State management:** Pinia
- **HTTP client:** Axios
- **Styling:** Tailwind CSS v4 (новый синтаксис)

## 📁 Структура проекта

```
SmartLeaves/
├── backend/                    # Django backend
│   ├── apps/
│   │   ├── categories/        # Модель категорий (2 уровня)
│   │   ├── tags/              # Модель тегов с auto-counting
│   │   ├── worksheets/        # Модель рабочих листов
│   │   └── cms/               # Wagtail CMS (статические страницы, настройки)
│   ├── config/                # Настройки Django
│   ├── media/                 # Загруженные файлы (PDF, изображения)
│   ├── manage.py
│   ├── db.sqlite3
│   └── requirements/
├── frontend/                   # Vue.js frontend
│   ├── src/
│   │   ├── api/               # API клиенты
│   │   ├── components/        # Vue компоненты
│   │   ├── views/             # Страницы
│   │   ├── stores/            # Pinia stores
│   │   ├── types/             # TypeScript types
│   │   ├── router/            # Vue Router
│   │   └── style.css          # Tailwind v4 styles
│   ├── package.json
│   └── vite.config.ts
├── venv/                      # Python virtual environment
├── categories.json            # Структура категорий из JSON
└── PROJECT_CONTEXT.md         # Этот файл
```

## 🗂️ Ключевые модели данных

### Category (Категория)
- **Уровни:** Максимум 2 уровня (родитель → ребенок)
- **Поля:** name, slug, description, parent, icon, order, is_active
- **Валидация:** Запрет на создание 3-го уровня
- **Slug:** Автогенерация из Cyrillic/Latin
- **Метод:** `get_worksheets_count()` - подсчет worksheet

**Примеры категорий:**
- Alphabets (3 подкатегории)
- Numbers (2 подкатегории)
- Maths (6 подкатегорий)
- Vocabulary (9 подкатегорий)
- Shapes, Coloring, Drawing... (без подкатегорий)

### Tag (Тег)
- **Поля:** name, slug, description, usage_count
- **Auto-counting:** usage_count обновляется автоматически через signals
- **ManyToMany:** Связь с Worksheet

**Примеры тегов:**
- Age 3-4, Age 5-6, Age 7-8
- Preschool, Grade 1, Grade 2
- Easy, Medium, Hard
- Colorful, Black & White

### Worksheet (Рабочий лист)
- **Поля:** title, slug, description, category, grade_level, difficulty
- **Файлы:**
  - `pdf_file` - основной PDF файл
  - `thumbnail` - миниатюра 300x400px (для каталога)
  - `preview_image` - превью 800x1000px (для детальной страницы)
- **Статистика:** views_count, downloads_count
- **Автогенерация превью:** При сохранении PDF создаются изображения
- **Signals:** Автообновление usage_count тегов

### SiteSettings (Глобальные настройки)
- **Поля:** contact_email, contact_phone, header_text, footer_text
- **Social media:** telegram_url (Facebook/Instagram удалены)
- **Pagination:** worksheets_per_page = 21 (делится на 3)
- **Управление:** Через Wagtail админку

## 🌐 API Endpoints

### Категории
- `GET /api/categories/` - Плоский список категорий
- `GET /api/categories/tree/` - Дерево категорий (для меню)
- `GET /api/categories/{slug}/` - Детали категории

### Теги
- `GET /api/tags/` - Список всех тегов
- `GET /api/tags/popular/` - Топ-20 популярных тегов
- `GET /api/tags/{slug}/` - Детали тега

### Worksheet
- `GET /api/worksheets/` - Список с фильтрацией и пагинацией
  - Параметры: `category__slug`, `tags__slug`, `grade_level`, `difficulty`, `search`, `page`, `page_size`
- `GET /api/worksheets/{slug}/` - Детали worksheet (увеличивает views_count)
- `GET /api/worksheets/{id}/download/` - Скачивание PDF (увеличивает downloads_count)

### Настройки
- `GET /api/settings/` - Глобальные настройки сайта

### Документация
- `GET /api/docs/` - Swagger UI
- `GET /api/redoc/` - ReDoc
- `GET /api/schema/` - OpenAPI schema

## 📄 Frontend страницы

### HomePage (`/`)
- Intro текст из settings
- 8 последних worksheet
- Список всех категорий с детьми

### WorksheetListPage (`/category/{slug}` или `/tag/{slug}`)
- Layout: 70% worksheet список + 30% теги sidebar
- Пагинация (21 worksheet на странице = 7 рядов по 3)
- Breadcrumbs навигация
- Фильтрация по категории или тегу

### WorksheetDetailPage (`/worksheet/{slug}`)
- Preview изображение (800x1000px)
- Overlay с затемнением (10% → 40% при hover)
- Иконка скачивания всегда видна
- Кнопка "Скачать PDF"
- Статистика (views, downloads)
- Теги с ссылками

### StaticPage (`/contacts`, `/about`, `/terms`)
- Простые страницы для статического контента
- Интеграция с Wagtail CMS (планируется)

## 🎨 UI/UX Особенности

### Навигация (AppHeader)
- Логотип и слоган из settings
- Категории 1 уровня - горизонтально в 2 строки
- Категории с детьми - выпадающее меню при hover
- Счетчики worksheet в скобках

### Карточки Worksheet
- Grid: 3 колонки на desktop
- Thumbnail 300x400px
- Title, description (line-clamp-2)
- Категория, теги (первые 3)
- Статистика (просмотры, скачивания)

### Детальная страница
- Preview 800x1000px с постоянным легким затемнением
- При hover: затемнение усиливается
- Иконка скачивания всегда видна
- Кнопка "Скачать PDF" под изображением

### Пагинация
- 21 элемент на странице (делится на 3)
- Smart pagination с "..." для больших списков
- Previous/Next кнопки

## 🔧 Важные детали реализации

### Tailwind CSS v4
- Используется новый синтаксис: `@import "tailwindcss"`
- Кастомные цвета через `@theme {}` block
- `postcss.config.js` использует `@tailwindcss/postcss`

### Image URLs
- Django REST Framework автоматически возвращает **полные URL** для ImageField
- Frontend использует URL напрямую из API: `worksheet.thumbnail`
- **НЕ НУЖНО** добавлять `VITE_API_URL` перед путями изображений!

### Фильтрация API
- По slug категории: `?category__slug=alphabets`
- По slug тега: `?tags__slug=age-3-4`
- Django FilterSet использует двойное подчеркивание для связей

### Signals для auto-counting
- При добавлении/удалении worksheet к тегу → обновляется `usage_count`
- При публикации/снятии с публикации → обновляется `usage_count`

### Генерация Preview
- Команда: `python manage.py generate_previews`
- Использует Pillow для создания красивых preview
- Рандомные цвета для разнообразия
- Автоматически при создании через команду `generate_100_worksheets`

## 🚀 Запуск проекта

### Backend
```bash
cd backend
source ../venv/bin/activate  # или venv\Scripts\activate на Windows
python manage.py runserver
```
Доступен на: http://127.0.0.1:8000

**Админки:**
- Wagtail CMS: http://127.0.0.1:8000/admin/ (admin/admin123)
- Django Admin: http://127.0.0.1:8000/django-admin/

### Frontend
```bash
cd frontend
npm run dev
```
Доступен на: http://localhost:5173

## 📊 Текущее состояние данных

- **Worksheet:** 900 (400 готовых + 500 генерируются)
- **Категории:** 40 (24 родительских + 16 дочерних)
- **Теги:** 24
- **PDF файлы:** Все с уникальным дизайном
- **Preview изображения:** Все сгенерированы (thumbnail + preview)

## 🛠️ Management команды

### Загрузка категорий
```bash
python manage.py load_categories_from_json
```
Загружает категории из `categories.json` с правильной сортировкой по полю `order`.

### Генерация worksheet
```bash
python manage.py generate_100_worksheets --count=300
```
Генерирует указанное количество worksheet с PDF и preview изображениями.

### Генерация preview
```bash
python manage.py generate_previews
```
Генерирует preview изображения для всех worksheet без них.

## 🐛 Решенные проблемы

1. **Tailwind CSS v4 PostCSS Error**
   - Решение: Использовать новый синтаксис `@import "tailwindcss"` и `@theme {}`

2. **Vite Module Resolution**
   - Решение: Добавить alias в `vite.config.ts`

3. **SiteSettings DoesNotExist**
   - Решение: Создать Site и SiteSettings через Django shell

4. **Categories API TypeError**
   - Решение: Извлекать `data.results` из пагинированного ответа

5. **Image URLs дублирование**
   - Решение: DRF уже возвращает полные URL, не добавлять VITE_API_URL

6. **Preview overlay блокирует картинку**
   - Решение: Использовать `pointer-events-none` и разделить overlay на слои

7. **Фильтрация по slug вместо ID**
   - Решение: Добавить `category__slug` и `tags__slug` в filterset_fields

## 📝 TODO / Возможные улучшения

- [ ] Интеграция статических страниц с Wagtail
- [ ] Переход на PostgreSQL для production
- [ ] Добавление поиска с highlight
- [ ] SEO оптимизация (meta tags, sitemap)
- [ ] Перевод категорий на русский язык (по желанию)
- [ ] Добавление фавиконов и og:image
- [ ] Настройка CORS для production
- [ ] Docker compose для development
- [ ] CI/CD pipeline
- [ ] Мониторинг и аналитика

## 🔐 Важные пароли и данные

**Django superuser:**
- Username: admin
- Password: admin123

**Environment variables (.env):**
- `SECRET_KEY`: адkyl=8!5nvh+3(*xx27l%_c(b9*-f8)tg4+f#d+otkmb#=d9h
- `DEBUG`: True
- `VITE_API_URL`: http://127.0.0.1:8000

## 📞 Контакты

- **Owner:** Artem Sitnikov
- **Email:** (указать при необходимости)
- **Project Location:** /Users/artemsitnikov/SmartLeaves/

---

**Дата создания контекста:** 2026-01-26
**Версия Django:** 5.0
**Версия Vue:** 3
**Версия Node:** (проверить с `node --version`)
**Версия Python:** 3.13
